/**
 * :) ;) :*( :P :D :( :o 8O
 */
let rangees = [0, 1, 2, 3, 4]
let colonnes = [0, 1, 0, 0, 0]
for (let y of rangees) {
    for (let x of colonnes) {
        led.plot(x, y)
    }
}
basic.forever(function () {
	
})
